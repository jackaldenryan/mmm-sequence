# Note: This project requires Tkinter, which is not installable via pip.
# On macOS with Homebrew: brew install python-tk@3.11
# On Ubuntu/Debian: sudo apt-get install python3-tk
# On Fedora: sudo dnf install python3-tkinter

click==8.1.3
graphviz==0.20.3
matplotlib==3.7.1
numpy==1.24.2
